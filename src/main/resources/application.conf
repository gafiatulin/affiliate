akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 8080
}

database {
    url = "jdbc:postgresql:db"
    user = "local_user"
    password = "password"
    driver = org.postgresql.Driver
    connectionPool = HikariCP
}